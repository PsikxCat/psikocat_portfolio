/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'icongr.am',
      'i.ibb.co',
      'img.icons8.com'
    ],
  },
}

module.exports = nextConfig
